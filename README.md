# Project Setup Instructions 

**Clone the repository** using the following command in PowerShell
 https://github.com/aniket12345678/jira_frontend.git

Create a .env file in the main directory and paste the frontend environment file provided in the email

Install the node packages using the below command in the terminal:

### `npm i`

Run the project using the below command in the terminal:

### `npm start`
